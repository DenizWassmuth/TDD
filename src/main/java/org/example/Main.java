package org.example;

import org.example.Utils.Test;

public class Main {
    static void main() {

        // diese Nachricht habe ich mit EDit geschrieben!

        System.out.println("\n" + Test.fizzBuzz(55));
    }

    // continuous Integration
    // -> plugin welches das Projekt in GitHub packt und ausführt (testet)
    // -> nütz vor allem der Sichtbarkeit für andere ob der Code funktionier toder nicht
}
